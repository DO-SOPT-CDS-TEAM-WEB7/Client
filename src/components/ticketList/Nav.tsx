import React from 'react';
import styled from 'styled-components';

import { LogoImg } from '../../assets/image';

const Nav = () => {
  return <St.NavContainer></St.NavContainer>;
};

export default Nav;

const St = {
  NavContainer: styled.nav`
    padding: 3.6rem 16rem;
  `,
};
